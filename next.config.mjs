/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "t3.ftcdn.net",
            "www.intervaledu.com",
            "images.ctfassets.net",
            "localhost",
            "static.outschool.com",
        ],
    },
};

export default nextConfig;
